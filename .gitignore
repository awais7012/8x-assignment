node_modules
.next
out
build
.DS_Store
*.tsbuildinfo
next-env.d.ts
.env*
!.env.example

# Agent capture: the session logs themselves DO ship (.agent-logs/*.md).
# Only the hook's own bookkeeping is excluded — the turn counter and the
# hook's error sidecar, neither of which is part of the prompt/response record.
.agent-logs/.capture-state.json
.agent-logs/.capture-errors.log
.vercel
.env*.local
